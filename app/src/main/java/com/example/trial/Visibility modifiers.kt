package com.example.trial

/* Public can be visible everywhere
Private only visible within the class not even to the subclass
internal visible everywhere inside the same module
Protected when a class has an protected member and when this class object is created in other class and when we try to
access the protected variable means it will throw error. We can access it when it is inherited
 */